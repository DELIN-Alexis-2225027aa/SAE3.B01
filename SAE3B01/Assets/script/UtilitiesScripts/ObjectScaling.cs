using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObjectScaling : MonoBehaviour
{/*
    float newObjectXSize;
    float newObjectYSize;
    float screenWidth;
    float screenHeight;

    public void scaleYAxisObject(RectTransform objectToScale, float YscalePercentage)
    {
        screenWidth = Screen.width;
        newObjectYSize = screenHeight * YscalePercentage / 100f;
        objectToScale.sizeDelta = new Vector2(newObjectXSize, objectToScalesizeDelta.x);
    }

    public void scaleXAxisObject(RectTransform objectToScale, float XscalePercentage, float YscalePercentage)
    {
        screenWidth = Screen.width;
        newObjectXSize = screenWidth * XscalePercentage / 100f;
        objectToScale.sizeDelta = new Vector2(newObjectXSize, objectToScale.sizeDelta.y);
    }

    public void scaleObject(RectTransform objectToScale, float XscalePercentage, float YscalePercentage)
    {
        screenWidth = Screen.width;
        newObjectXSize = screenWidth * XscalePercentage / 100f;
        newObjectYSize = screenHeight * YscalePercentage / 100f;
        objectToScale.sizeDelta = new Vector2(newObjectXSize, newObjectYSize);
    }
    */
}
